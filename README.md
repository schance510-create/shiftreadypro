# Shift Ready — Setup Instructions

## What you have
- `index.html` — the main app (frontend)
- `netlify/functions/` — backend API functions (Stripe + Supabase)
- `netlify.toml` — Netlify configuration
- `supabase_setup.sql` — paste this into Supabase to create your database

## Setup Order
1. Supabase (database)
2. Netlify (hosting)
3. DNS (subdomain)
4. Test it
